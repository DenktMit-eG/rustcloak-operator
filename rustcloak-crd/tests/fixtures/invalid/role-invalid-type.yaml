apiVersion: rustcloak.k8s.eboland.de/v1beta1
kind: KeycloakRole
metadata:
  name: my-role
spec:
  realmRef: my-realm
  definition:
    name: my-role
    composite: "not-a-boolean"  # Should be boolean
